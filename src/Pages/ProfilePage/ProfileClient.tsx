import {useEffect,useState } from 'react';
import { Favourites } from '../../entities/movie/api/types';

function CreateProfileClient (){
    const [favourites,setFavorites] = useState<Favourites[]>([]);

    
}